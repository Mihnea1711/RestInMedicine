export class AppointmentData {
    constructor(idProgramare, idPatient, idDoctor, date, status) {
        this.idProgramare = idProgramare;
        this.idPatient = idPatient;
        this.idDoctor = idDoctor;
        this.date = date;
        this.status = status;
    }
}